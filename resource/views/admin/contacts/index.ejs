<div class="d-flex justify-content-between align-items-center mb-3 border-bottom-1">
    <h2>پیام کاربران در صفحه تماس با ما</h2>
</div>
<% if(contacts.docs.length ){ %> 
<% contacts.docs.forEach(contact => { %> 
<div class="contact_us mt-4 mb-4">
    <div class="row">
        <div class="col-12 col-md-4">
            <p>نام : <span><%= contact.name %> </span></p>
        </div>
        <div class="col-12 col-md-4">
            <p>ایمیل : <span><%= contact.email %></span></p>
        </div>
        <div class="col-12 col-md-4">
            <p>شماره موبایل : <span><%= contact.phone %></span></p>
        </div>
    </div><!--row-->
    <div class="row mt-4">
        <div class="col-12 col-md-12">
            <p>متن پیام کاربر: <span><%= contact.body %></span></p>
        </div>
    </div>
</div>
<% }); %>
<% }else{ %>
    <div class="row mt-5">
        <div class="col-12">
            <div class="not-contact">
                <p>فعلا پیامی دریافت نکرده اید...</p>
            </div>
        </div>
    </div> 
<% } %>

