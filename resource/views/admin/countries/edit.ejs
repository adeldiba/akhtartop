<div class="d-flex justify-content-between align-items-center mb-3 pb-2 border-bottom">
    <h2>ویرایش کشور سازنده</h2>
</div>
<form class="form-horizontal" action="/admin/countries/<%= country._id %>?_method=put" method="post">

    <%- include(viewPath('layouts/error-messages')) -%>

    <div class="form-group row">
        <div class="col">
            <label for="name" class="control-label font-weight-bold">نام کشور</label>
            <input type="text" class="form-control" name="name" value="<%= country.name %>" id="name">
        </div>
    </div>
    <div class="form-group row">
        <div class="col">
            <label for="lang" class="control-label font-weight-bold font-weight-bold">زبان نمایشی</label>
            <select name="lang" id="lang" class="form-control">
                <option value="fa" <%= country.lang == 'fa' ? 'selected' : '' %> >زبان فارسی</option>
                <option value="tr" <%= country.lang == 'tr' ? 'selected' : '' %>>زبان ترکی</option>
            </select>
        </div>
</div>

<div class="form-group row">
    <div class="col">
        <label for="parent" class="control-label font-weight-bold font-weight-bold">دسته مربوطه</label>
        <select name="parent" id="parent" class="form-control">
            <option value="none">دسته اصلی</option>
            <% countries.forEach(coun => { %> 
                <option value="<%= coun.id %>" <%= String(old('countries' , coun.parent)) == String(countries.id) ? 'selected' : '' %> ><%= coun.name %></option>
            <% }) %>
        </select>
    </div>
</div>
    <div class="form-group row">
            <div class="col">
                <button type="submit" class="btn btn-danger">ارسال</button>
            </div>
        </div>
</form>